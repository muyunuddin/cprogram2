name: hello world
on:
 push:
  branches:
   - main
 jobs:
   hello_world_job:
    runs-on: ubuntu-latest
    steps:
     - name: checkout code
       uses: actions/checkout@v3
     - name: print message
       run: echo "this is to test git hub actions"
